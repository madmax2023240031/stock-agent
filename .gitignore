# 가상환경
venv/
.venv/

# 환경변수 (API 키 포함 — 절대 커밋하지 않음)
.env
.env.*

# Python 캐시
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python

# 빌드 / 배포
*.egg-info/
dist/
build/

# IDE
.idea/
.vscode/
*.swp
*~

# macOS
.DS_Store

# KIS API 캐시 (토큰 포함 — 절대 커밋하지 않음)
.dart_corp_codes.json
.kis_token_cache.json

# 거래 로그 (개인 거래기록 — 절대 커밋하지 않음)
trade_log.json
trade_log_TEST.json
auto_trader_dryrun_log.json
kill_switch_state.json
kill_switch_state_TEST.json

# 노션이 원본인 문서 (저장소 미포함 원칙)
stock-agent-사용설명서.md
stock-agent-자동매매로드맵.md

# 유니버스 디스크 캐시 (작업 큐 ② — 로컬 상태 파일)
universe_cache.json
